const path = require('path');

module.exports = {
    // 配置存储路径 by path_module
    screenshot: path.resolve(__dirname, '../../screenshot'),
    aimBaiduImages: path.resolve(__dirname, '../../aimBaiduImages'),
};